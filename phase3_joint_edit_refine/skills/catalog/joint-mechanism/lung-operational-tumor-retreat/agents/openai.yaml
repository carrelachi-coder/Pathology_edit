interface:
  display_name: "lung-operational-tumor-retreat"
  short_description: "Shadow-only certified mask mechanism"
  default_prompt: "Use $lung-operational-tumor-retreat only to select compiler-certified mask candidates; never infer execution authority from H&E."
