---
name: breast-cohesive-nst-front
description: Deprecated Breast mechanism retained for historical audit only. It is closed because it required general H&E front recognition; use the annotation-anchored Breast mechanisms instead.
---

# Breast cohesive joint editing

1. Read `references/joint_contract.json` before selecting an interface or cell layout.
2. Do not execute this mechanism. Route broad boundary growth to `breast-annotation-anchored-boundary-growth`, explicit cord/strand intent to `breast-cell-seeded-invasive-cord`, and explicit detached nest/island intent to `breast-peritumoral-tumor-nest`.
3. Compile the tissue and cell programs as one candidate. Never approve either mask independently.
4. Read `references/evidence.json` and `references/counterexamples.json` when recognition is uncertain.
5. Treat every render-only claim as a post-generation critic requirement, not a mask guarantee.
